@foreach (var tag in tags)
{
    <TemplateTag tag="@tag" OnTagClick="@OnTagClick"></TemplateTag>
    @if (tag != tags.Last())
    {
        @(", ")
    }
}

@code {
    [Parameter]
    public string[] tags { get; set; }
    [Parameter]
    public EventCallback<string> OnTagClick { get; set; }
}
